/**
 * Shared UI components and theme for Deep Reps.
 *
 * Contains the Material 3 theme definition, design tokens (colors, typography, spacing),
 * and reusable Compose components (buttons, cards, input fields, timer display).
 * All shared components are prefixed with "DeepReps" (e.g., DeepRepsButton).
 */
package com.deepreps.core.ui
