/**
 * Repository interfaces for the domain layer.
 *
 * Implementations live in [:core:data]. The domain layer defines contracts only.
 * All repository methods are suspend functions or return Flow.
 */
package com.deepreps.core.domain.repository
